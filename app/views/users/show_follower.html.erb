<% @users.each do |user| %>

<h1>followers</h1>

<table>
<tr>
  <td>  
  <%= gravatar_for @user %>
  <%= user.name %> 
  </td>
  <td><%= link_to '詳細', user_path(user) %></td>
</tr>

</table>
<% end %>